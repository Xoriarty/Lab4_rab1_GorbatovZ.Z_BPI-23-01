<Window x:Class="Lab4_rab1_GorbatovZ.Z_BPI_23_01.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab4_rab1_GorbatovZ.Z_BPI_23_01.View"
        mc:Ignorable="d"
        Title="WindowNewEmployee" Height="300" Width="400">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Label Grid.Row="1" Grid.Column="0" 
               Content="Код:" 
               HorizontalAlignment="Right"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding Person.Id, Mode=OneWay}" 
                 IsEnabled="False" 
                 Background="#FFF0F0F0"/>

        <Label Grid.Row="2" Grid.Column="0" 
               Content="Должность:" 
               HorizontalAlignment="Right"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Name="CbRole"
          DisplayMemberPath="NameRole"
          ItemsSource="{Binding}"
          SelectedValue="{Binding Person.RoleName}"
          SelectedValuePath="NameRole"/>

        <Label Grid.Row="3" Grid.Column="0" 
               Content="Фамилия:" 
               HorizontalAlignment="Right"/>
        <TextBox Name="txtLastName" Grid.Row="3" Grid.Column="1" 
                 Text="{Binding Person.LastName, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="4" Grid.Column="0" 
               Content="Имя:" 
               HorizontalAlignment="Right"/>
        <TextBox Name="txtFirstName" Grid.Row="4" Grid.Column="1" 
                 Text="{Binding Person.FirstName, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="5" Grid.Column="0" 
               Content="Дата рождения:" 
               HorizontalAlignment="Right"/>
        <DatePicker Grid.Row="5" Grid.Column="1" 
                    SelectedDate="{Binding Person.Birthday}"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" />
            <Button Content="Отменить" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>
